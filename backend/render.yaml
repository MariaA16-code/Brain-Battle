services:
  - type: web
    name: brainbattle-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python manage.py migrate && gunicorn core.wsgi --bind 0.0.0.0:$PORT
    rootDir: backend